[project]
name = "gocamnet-py"
version = "0.1.0"
description = "PyTorch inference server for GoCamNet"
requires-python = ">=3.10"
dependencies = [
  "torch>=2.3",
  "ultralytics>=8.2",
  "numpy>=1.24",
  "msgpack>=1.0",
]

[tool.uv]

